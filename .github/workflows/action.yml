name: Publish

on:
  push:
    tags:
      - '*' 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 16

      - name: Install dependencies
        run: npm install

      - name: Build zip file
        run: npm run build

      - name: Upload to Chrome Web Store
        uses: mobilefirstllc/cws-publish@latest
        with:
          action: 'upload'  # one of: upload, publish, testers
          client_id: ${{ 993464563511-8ep955beucn73k5m859rf8r2g7fgnupf.apps.googleusercontent.com }}
          client_secret: ${{ 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 }}
          refresh_token: ${{ 1//03mbZLZa8GIPkCgYIARAAGAMSNwF-L9IraTTne6g63N9GOEHcgzokIVoAd6MRbHuJCx1ioav8ZR4BlSSiBrbhl6KljF3TlLtDa-M }}
          extension_id: 'leaeklepgoiehjffeeoajaegpldpjenm' 
          zip_file: 'linkedin.zip'
